name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  # FSD structure + CSS governance + typecheck/build
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: FSD structure (steiger)
        run: pnpm check:fsd
      - name: CSS color governance (stylelint)
        run: pnpm check:style
      - name: Unit tests + coverage gate (vitest)
        run: pnpm test:coverage
      - name: Typecheck + build
        run: pnpm build

  # Catalog load benchmark — fails the pipeline on perf regression
  perf:
    needs: checks
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - name: Checkout content data
        uses: actions/checkout@v7
        with:
          repository: Nghiathan13/ownlish-data
          path: ownlish-data
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Benchmark catalog load
        run: node scripts/bench-catalog.mjs ownlish-data/catalog.json
      - name: Update baseline (main pushes only, on pass)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          node scripts/bench-catalog.mjs ownlish-data/catalog.json --update-baseline
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/perf-baseline.json
          git diff --cached --quiet || {
            git commit -m "ci: update perf baseline [skip ci]"
            git push
          }

  # Deploy gate — only runs when checks AND perf pass (push events only)
  deploy:
    needs: [checks, perf]
    if: (github.ref == 'refs/heads/main' && github.event_name == 'push') || startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Linux system dependencies (Tauri 2)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            librsvg2-dev \
            libayatana-appindicator3-dev \
            patchelf \
            build-essential \
            pkg-config
      - uses: pnpm/action-setup@v6
        with:
          version: 11
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ./src-tauri -> target
      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile
      - name: Build release bundle
        run: pnpm tauri build
      - name: Upload bundle
        uses: actions/upload-artifact@v7
        with:
          name: ownlish-linux
          path: src-tauri/target/release/bundle/
          if-no-files-found: error
